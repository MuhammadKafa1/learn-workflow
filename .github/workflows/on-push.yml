on:
  push:
    branches:
      - test-branch-13-11-2023


name: on-push-to-test-branch-13-11-2023
jobs:
  build:
    name: on push
    strategy:
      matrix:
        environment: [dev, stage, prod]
    environment: ${{ matrix.environment }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Write a message for on pull request type open
        env:
          ENV_VAR: ${{ vars.env_var }}
        run: |
          echo "This demo1 file for pull request open reopen synchronize and show.$ENV_VAR"